import { EN, RU } from '../../contexts/languages';
import { LangContext } from '../../contexts/types';

export const contextEn: LangContext = {
  lang: EN,
  setLang: (): void => {},
};

export const contextRu: LangContext = {
  lang: RU,
  setLang: (): void => {},
};
